# Public project endpoint + publishable key — these are safe to expose (they're
# already shipped in the QuickFind Inventory app's client bundle) and default
# to the real project inside the server if unset, but you can override here.
SUPABASE_URL=https://ygwgkoaoojkaabsieqpf.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_yz4U8vfZYRk8HGdPPBPugQ_BMF-BkHr

# Real secret — required for every staff-only tool (creating/updating inventory,
# managing marketplace listings, reading/resolving the intake queue). Without it
# those tools return a clear error and only the public read/customer-intake
# tools work.
#
# Get this from the Supabase dashboard for project ygwgkoaoojkaabsieqpf:
# Project Settings -> API -> service_role secret key. Never commit this value.
SUPABASE_SERVICE_ROLE_KEY=
